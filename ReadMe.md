#to upload image to s3. create bucket,
create im user,
 set bucket policy,
 create group user with allowed ploicies, cloudformating (programin) and fulls3access and access to s3,

 set cors in bucket, 
 set policy in buket